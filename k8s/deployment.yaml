apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-itsa
spec:
  replicas: 1
  selector: { matchLabels: { app: hello-itsa } }
  template:
    metadata: { labels: { app: hello-itsa } }
    spec:
      containers:
      - name: app
        image: alpine:3.20
        command: ["sh","-c","echo Hello ITSA-5501 && sleep 3600"]
